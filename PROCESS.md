# 智能直播切片 完整处理流程

从前端上传视频到最终输出切片的全流程说明。

---

## 🔹 第一步：前端上传阶段
1.  用户在网页上**拖拽或点击**选择视频文件，支持所有ffmpeg可解码的常见格式：`.mp4/.mov/.mkv/.flv/.ts/.avi/.m4v/.webm`
2.  前端使用 `XMLHttpRequest` 将文件以 `multipart/form-data` 流式上传到后端 `/api/upload` 接口
3.  后端分块（1MB/块）保存文件到 `data/uploads/<文件名>`，重名自动加 `(1)/(2)` Windows风格后缀，不会覆盖旧文件
4.  上传过程实时显示进度条（已传大小/总大小/百分比）
5.  上传完成后，后端用 `ffprobe` 探测视频时长、大小，返回文件信息
6.  前端显示"已选择视频"信息，"开始切片"按钮启用，可调整切片参数（所有参数默认值为平台最优，普通用户无需调整）：
    - **切片数量**：智能推荐（默认，豆包根据内容密度自动决定条数）/1/2/3/5/8/10条可选
    - **烧录字幕**：勾选则字幕硬嵌入视频画面，默认不勾选，始终生成独立SRT字幕文件
    - **快速预览**：勾选则低画质快速渲染、跳过QC质检，速度快3~5倍仅用于查看选段效果
    - ⚙️ **高级选项**（默认收起）：
      - 调色风格：智能自然微调(auto，默认)/原色调无修改(none)/轻度画质增强(light)/暖调电影感(warm_cinematic)
      - 单条时长范围：双滑块调整，默认30秒~5分钟，10秒~10分钟区间可调
      - ASR引擎：默认火山引擎云ASR（高速高精度），支持切换Qwen3-ASR本地开源模型（离线/隐私/超长音频/方言场景，需额外安装依赖）

---

## 🔹 第二步：任务启动
1.  用户点击"🚀 开始切片"按钮，前端调用 `/api/start` 接口，传入视频路径和所有配置参数
2.  后端验证：文件存在、格式支持、ffmpeg/API密钥都已配置
3.  自动判断输出目录：
    - 新上传视频：创建唯一输出目录 `data/output/<视频名>/`，重名自动加`(1)/(2)`后缀
    - 历史任务重剪：复用原有输出目录，不新建重复文件夹
4.  自动复用同视频缓存：如果其他任务目录已有该视频的转录、打包结果，自动复制到当前目录，跳过重复转录/打包，省时间省费用
5.  后台线程启动切片流水线，前端立即切换到进度页，开始每秒轮询 `/api/status` 查看进度
6.  历史任务支持🔄**重剪**功能：点击后自动回到配置页，填充原视频和所有参数，可自由修改后启动，不需要重新上传视频。

---

## 🔹 第三步：核心处理流水线（4个阶段）
流水线总进度按阶段权重计算：转录55% → 打包5% → 选段15% → 渲染25%

### 🎤 阶段1：转录（0-55%）
1.  用ffmpeg从原视频提取音频（16kHz单声道、16-bit PCM WAV格式，两种ASR通用最优输入格式）
2.  **双引擎可选**：
    - 默认**火山引擎ASR极速版**：长于60分钟的视频自动分段，避免超过HTTP 413大小限制（单段安全阈值~60分钟，留足余量），获取词级时间戳、说话人分离结果，以及带自动标点的句级文本；15分钟视频转录约15秒完成，需要网络和API密钥
    - 可选**Qwen3-ASR本地开源模型**：无视频长度限制无需分段，支持中文方言、带BGM/歌声识别，完全离线运行无需网络，适合隐私场景；0.6B模型需4GB显存，1.7B高精度模型需8GB显存，首次运行自动下载模型权重
3.  转录结果格式完全统一，包含词级时间戳和句级带标点文本，两种引擎输出数据契约100%兼容，下游打包/选段/渲染流程无需修改
4.  转录结果缓存到 `output/<视频名>/transcripts/<视频名>.json`，重复运行/重剪直接复用缓存，不重复调用API/模型省时间省费用
5.  跨任务自动复用：同视频在不同任务/重剪时自动扫描已有转录结果复制复用，避免重复工作。

### 📦 阶段2：打包（55-60%）
1.  直接复用ASR返回的带自然标点的句级结果，结合0.5秒以上静音或说话人切换规则分组为短语级文本块
2.  自动拆行长句为最多2行、每行18个中文字符的字幕友好格式，保证标点永远在行尾，不会出现逗号/句号在行首的问题
3.  生成人类可读的markdown文件 `takes_packed.md`，格式：`[开始-结束] S<说话人ID> 文本内容`
4.  打包后文件大小约为原始JSON的1/10，大幅减少LLM token消耗，打包缓存永久复用。

### 🤖 阶段3：豆包选段（60-75%）
1.  调用 **doubao-seed-2-0-mini-260428** 模型，传入打包文本+选段规则prompt
2.  自动条数模式动态计算合理数量：
    - <10分钟短视频：不少于1条
    - 10~30分钟中视频：不少于2条
    - ≥30分钟长直播：不少于3条
    - 条数上限：<2小时视频最多12条，≥2小时超长直播最多16条
    - 参考密度：每小时5~8条，金句密集可多切，内容平淡可少切，绝对不凑水货片段
3.  选段优先级（按重要性排序）：开头3秒钩子（必须是金句/爆点/反常识观点，禁止以口癖/发语词开头）> 金句/干货结论 > 冲突/反转/情绪高点 > 内容完整
4.  支持**多段拼接**：同一论点分散在视频不同位置时，自动拼成一条逻辑连贯的切片；切点自动吸附到句子边界，前后留50ms/80ms缓冲，防止切到词头/词尾吞字
5.  输出EDL（编辑决策表）到带时间戳的clips目录：`clips_YYYYMMDD_HHMMSS/edl_multi.json`，包含每条切片的标题、选段理由、多段时间码
6.  `clips` Windows目录联接（快捷方式）永远指向最新一次生成的结果目录，旧版本自动保留不覆盖
7.  选段约30~90秒完成。

### 🎞️ 阶段4：渲染（75-100%）
逐条渲染切片，每条切片经过以下处理：
1.  **参数提取**：根据EDL时间码自动应用切点padding，横竖屏自动适配分辨率（1080p，保持原视频比例不变形）
2.  **片段抽取**：用ffmpeg抽取每个片段，应用：
    - 4种调色预设可选：
      * `auto`（默认）：采样帧自动微调亮度/对比度/饱和度，幅度≤8%自然无痕迹
      * `light`：轻度画质增强，+3%对比度、-2%饱和度，画面更通透
      * `warm_cinematic`：暖调电影感，+12%对比度、压黑、暖阴影冷高光、filmic曲线
      * `none`：原色调无修改
    - 30ms音频淡入淡出，消除切点爆音
    - HDR视频自动做色调映射转SDR，避免上传平台后发灰过曝
3.  **拼接**：多段片段使用150ms视频交叉淡入+100ms音频交叉淡化，消除硬切跳变
4.  **字幕生成**：无论是否烧录字幕，始终生成逐句SRT字幕文件（微软雅黑粗体+2px黑边，竖屏自动上移避开平台底部UI遮挡）；勾选烧录时用libass滤镜硬嵌入视频画面
5.  **响度归一**：两遍EBU R128响度归一到-14 LUFS / -1 dBTP，符合抖音/B站/视频号所有主流平台音量标准，不会忽大忽小
6.  **写入文件**：输出最终 `clip_001.mp4`/`clip_001.srt` 到当前时间戳clips目录
7.  **✨ QC自动质检与修复**：
    - 多段拼接的切点±1.5秒生成胶片条+波形图
    - 发给**豆包vision模型**检测：仅判定严重影响观看的问题（明显画面跳变/削波爆音/黑场花屏/字幕完全遮挡），正常说话尖峰、轻微波动不报错，误报率极低
    - 发现问题自动调整padding/淡入淡出/切点偏移重试，最多3次自动修复
    - 仍有问题标记⚠️警告提示人工复看，不影响其他切片输出
8.  单条20~40秒的切片渲染约10~20秒
9.  渲染完成后自动清理 `clips_graded/clips_preview/clips_draft` 临时分段目录，输出目录干净无残留。

---

## 🔹 第四步：结果输出
1.  所有切片渲染完成后，生成 `manifest.json` 清单到当前时间戳clips目录，包含每条切片的标题、理由、时长、文件名、QC标记、本次任务所有配置参数
2.  前端自动切换到结果页：
    - 显示总条数、总时长
    - 每条切片可直接在浏览器内播放预览（自动挂载同目录SRT字幕）
    - 支持下载MP4、下载SRT字幕
    - "打开输出文件夹"按钮直接打开当前最新版clips目录
3.  **历史记录**：首页底部自动显示所有历史切片任务，自动扫描每个视频目录下所有`clips*`开头的结果，取最新修改的版本展示：
    - 📂 打开文件夹、▶ 查看切片、🔄 重剪、🗑️ 删除功能
    - 同视频所有历史重剪版本都保留在对应时间戳clips目录中，不会覆盖旧结果
4.  重剪任务自动复用转录+打包缓存，直接从选段阶段开始跑，15分钟视频重剪仅需1~2分钟出结果。

---

## 📂 最终目录结构
```
data/
├── uploads/                     # 上传的原视频保存位置，重名自动加(1)/(2)后缀
│   └── 飞书20260728-221221.mp4
└── output/                      # 所有输出统一根目录
    └── 飞书20260728-221221/     # 每个视频一个文件夹，重剪复用不新建
        ├── transcripts/         # 转录缓存（所有版本共用）
        │   └── 飞书20260728-221221.json
        ├── takes_packed.md      # 打包文本缓存（所有版本共用）
        ├── clips -> clips_20260730_183000/  # Windows目录联接指向最新版本
        ├── clips_20260729_175617/            # 第一次生成的结果
        │   ├── edl_multi.json
        │   ├── clip_001.mp4
        │   ├── clip_001.srt
        │   ├── verify/          # QC用胶片条+波形图
        │   └── manifest.json
        └── clips_20260730_183000/            # 重剪生成的新结果
            ├── edl_multi.json
            ├── clip_001.mp4
            ├── clip_001.srt
            ├── verify/
            └── manifest.json
```

---

## ⏱️ 总耗时参考
- 15分钟视频从上传完成到出结果约3~5分钟（转录15s + 选段1min + 渲染1~3min）
- ASR和LLM费用：转录约几分钱/小时，选段token消耗极小，成本很低
- 已有转录缓存的视频重剪：跳过转录+打包阶段，直接从选段开始，约1~2分钟出结果
- 快速预览模式：渲染速度提升3~5倍，适合快速验证选段效果。
