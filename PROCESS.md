# 智能直播切片 完整处理流程

从前端上传视频到最终输出切片的全流程说明。

---

## 🔹 第一步：前端上传阶段
1. 用户在网页上**拖拽或点击**选择任意目录下的 `.mp4` 文件
2. 前端使用 `XMLHttpRequest` 将文件以 `multipart/form-data` 流式上传到后端 `/api/upload` 接口
3. 后端分块（1MB/块）保存文件到 `data/uploads/<文件名>.mp4`，重名自动加 `_1/_2` 后缀
4. 上传过程实时显示进度条（已传大小/总大小/百分比）
5. 上传完成后，后端用 `ffprobe` 探测视频时长、大小，返回文件信息
6. 前端显示"已选择视频"信息，"开始切片"按钮启用

---

## 🔹 第二步：任务启动
1. 用户点击"🚀 开始切片"按钮，可选择是否勾选"烧录字幕"
2. 前端调用 `/api/start` 接口，传入视频路径和字幕选项
3. 后端验证文件存在、是mp4格式、ffmpeg/API密钥都已配置
4. 自动创建**唯一输出目录**：`data/output/<视频名>/`（重名加后缀）
5. 后台线程启动切片流水线，前端立即切换到进度页，开始每秒轮询 `/api/status`

---

## 🔹 第三步：核心处理流水线（4个阶段）
流水线总进度按阶段权重计算：转录55% → 打包5% → 选段15% → 渲染25%

### 🎤 阶段1：转录（0-55%）
1. 用ffmpeg从原视频提取音频（16kHz单声道WAV）
2. 长于60分钟的视频自动分段，避免超过火山ASR的HTTP 413大小限制
3. 调用**火山引擎ASR极速版**，获取词级时间戳+说话人分离
4. 转录结果缓存到 `output/<视频名>/transcripts/<视频名>.json`，重复运行不会重复转录音（省费用）
5. 15分钟视频转录约15秒完成

### 📦 阶段2：打包（55-60%）
1. 读取词级转录JSON，按0.5秒以上静音或说话人切换，分组为短语级文本
2. 生成人类可读的markdown文件 `takes_packed.md`，格式：`[开始-结束] S<说话人ID> 文本内容`
3. 打包后文件大小约为原始JSON的1/10，减少LLM token消耗

### 🤖 阶段3：豆包选段（60-75%）
1. 调用 **doubao-seed-2-0-mini-260428** 模型，传入打包后的完整转录
2. LLM按语义选高光片段：金句、冲突点、干货内容、情绪高点
3. 自动判断切片条数（1小时约5-8条，3-16条之间，不凑数），单条时长30秒-5分钟
4. 支持**多段拼接**：同一论点分散在视频不同位置时，自动拼成一条逻辑连贯的切片
5. 切点自动吸附到短语边界，前后留50/80ms缓冲，防止切到词头/词尾吞字
6. 输出EDL（编辑决策表）到 `clips/edl_multi.json`，包含每条切片的标题、理由、多段时间码
7. 选段约30-90秒完成

### 🎞️ 阶段4：渲染（75-100%）
逐条渲染切片，每条切片经过以下处理：
1. **参数提取**：根据EDL的时间码，自动应用切点padding（默认前50ms/后80ms）
2. **片段抽取**：用ffmpeg抽取每个片段，应用：
   - auto调色：采样帧自动微调亮度/对比度，幅度≤8%，自然无痕迹
   - 30ms音频淡入淡出，消除切点爆音
   - 统一缩放到1080p
3. **拼接**：多段片段使用150ms视频交叉淡入+100ms音频交叉淡化，消除硬切跳变
4. **字幕生成**：如果勾选烧录字幕，生成逐句srt字幕（微软雅黑字体，整句不拆分）；否则生成独立srt文件
5. **响度归一**：两遍EBU R128响度归一到-14 LUFS，适合社交媒体平台发布
6. **写入文件**：输出最终 `clip_001.mp4`/`clip_001.srt` 到 `output/<视频名>/clips/`
7. **✨ QC自动质检与修复**：
   - 多段拼接的切点±1.5秒生成胶片条+波形图
   - 发给**豆包vision模型**检测：画面跳变、音频爆音、黑场花屏、字幕遮挡
   - 发现问题 → LLM自动给出调整方案（增大padding、加长淡入淡出、偏移切点）→ 自动重渲染
   - 最多重试3次，仍有问题则标记⚠️警告提示人工复看，不影响输出
8. 单条20-40秒的切片渲染约10-20秒

---

## 🔹 第四步：结果输出
1. 所有切片渲染完成后，生成 `manifest.json` 清单，包含每条切片的标题、理由、时长、文件名、QC标记
2. 前端自动切换到结果页：
   - 显示总条数、总时长
   - 每条切片可直接在浏览器内播放预览
   - 支持下载MP4、下载SRT字幕
   - "打开输出文件夹"按钮直接打开 `output/<视频名>/clips/` 目录
3. **历史记录**：首页底部自动显示所有历史切片任务，可展开查看、播放、打开之前的结果，不需要重新处理

---

## 📂 最终目录结构
```
data/
├── uploads/                     # 上传的原视频保存位置
│   └── 飞书20260728-221221.mp4
└── output/                      # 所有输出统一根目录
    └── 飞书20260728-221221/     # 每个视频一个文件夹
        ├── transcripts/         # 转录缓存
        │   └── 飞书20260728-221221.json
        ├── takes_packed.md      # LLM用打包文本
        └── clips/
            ├── edl_multi.json   # 选段决策
            ├── clip_001.mp4     # 切片视频
            ├── clip_001.srt     # 字幕
            ├── clip_002.mp4
            ├── ...
            ├── verify/          # QC用胶片条+波形图
            └── manifest.json    # 结果清单
```

---

## ⏱️ 总耗时参考
- 15分钟视频：从上传完成到出结果约3-5分钟（转录15s + 选段1min + 渲染1-3min）
- ASR和LLM费用：转录约几分钱/小时，选段token消耗很小，成本很低
- 已有转录缓存的视频重跑：跳过转录阶段，速度更快
